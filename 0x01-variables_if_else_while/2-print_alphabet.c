#include <stdio.h>
#include <stdlib.h>

/**
 * main - main function
 * Description: Takess a random number, compares last digit with the number 5
 * Return: 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}

	putchar('\n');
	return (0);
}
